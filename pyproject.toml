[project]
name = "hestia"
version = "0.1.0"
description = "Your Personal Server Hearth"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.13",
    "python-dotenv>=1.1.0",
]
